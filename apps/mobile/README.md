# Mobile app boundary

The MVP validates the responsive member journey through the shared web client.
A React Native shell is intentionally deferred until authentication and API
contracts stabilize. The API and information architecture are designed for that
client: Today, Groups, Calendar, and Profile map directly to native tabs.

