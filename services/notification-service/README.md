# Notification service

The API currently writes in-app notifications to the development repository.
Before private beta, publish notification jobs to a queue and implement idempotent
push and email consumers. SMS remains opt-in and is not required for launch.

